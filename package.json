{
  "name": "@nitedani/img-optimizer",
  "version": "0.0.11",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "turbo run build",
    "release": "pnpm build && bumpp package.json packages/*/package.json examples/*/package.json && pnpm -r publish --access=public"
  },
  "devDependencies": {
    "bumpp": "^8.2.1",
    "turbo": "^1.4.6"
  },
  "engines": {}
}
